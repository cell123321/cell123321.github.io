(()=>{const id=new URLSearchParams(location.search).get('id'),H=document.getElementById('job-heading'),D=document.getElementById('job-detail'),A=document.getElementById('apply-button');const esc=s=>String(s||'').replace(/[&<>"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[m]));const ul=a=>`<ul>${(a||[]).map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;fetch('jobs.json',{cache:'no-store'}).then(r=>r.json()).then(js=>{const j=js.find(x=>x.id===id);if(!j)throw 0;document.title=`${j.title} | Optima`;H.innerHTML=`<span class="eyebrow">${esc(j.industry)}</span><h1>${esc(j.title)}</h1><div class="job-meta"><span class="job-pill">${esc(j.location)}</span><span class="job-pill">${esc(j.workplace)}</span><span class="job-pill">${esc(j.type)}</span><span class="job-pill">${esc(j.salary)}</span></div>`;D.innerHTML=`<p class="lead">${esc(j.summary)}</p><h2>Key responsibilities</h2>${ul(j.responsibilities)}<h2>Requirements</h2>${ul(j.requirements)}<h2>Benefits and support</h2>${ul(j.benefits)}<p><strong>Closing date:</strong> ${esc(j.closing)}</p>`;A.href=`candidates.html?job=${encodeURIComponent(j.title)}#submit-cv`}).catch(()=>{H.innerHTML='<h1>Role not found.</h1>';D.innerHTML='<p>This vacancy may have closed.</p>'})})();