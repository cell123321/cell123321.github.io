# Google Backend Status

The backend has already been created and deployed.

For this release, follow `BACKEND_UPDATE_V1.1.md` to update the existing deployment. Do not create a second spreadsheet, Drive folder or Apps Script project.
